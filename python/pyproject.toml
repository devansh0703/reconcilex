[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "reconcilex"
version = "1.0.0"
description = "ReconcileX Python modules"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["recon*"]
